pnpm init ( )

pnpm install fastify zod argon2 dotenv (libs que serão utilizadas)

pnpm install -D typescript tsx @types/node (dev dependencies)

npx tsc --init (inicializar o typescript)

pnpm add fastify zod fastify-type-provider-zod (dependencias)

pnpm add @fastify/swagger @fastify/cors (pluguins fastify)

pnpm add @scalar/fastify-api-reference (docs)

pnpm add -D typescript tsx @types/node (tipagem do ts)

#### dependencias do drizze, pg e dotenv + types

pnpm add drizzle-orm pg dotenv
pnpm add -D drizzle-kit tsx @types/pg
